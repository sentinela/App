import React from 'react';
import { Link } from 'react-router';

export const Perfil = () => (
  <div className="container">
    <button>
      <Link to="/">Voltar</Link>
    </button>
  </div>
);

export default Perfil;
